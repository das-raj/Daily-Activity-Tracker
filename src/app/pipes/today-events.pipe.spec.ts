import { TodayEventsPipe } from './today-events.pipe';

describe('TodayEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new TodayEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
